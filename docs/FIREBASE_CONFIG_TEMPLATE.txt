# Firebase Configuration Template
# Fill this out as you complete Phase 0
# Copy to firebase-credentials.txt (which is gitignored)

# === Project Information ===
GCP_PROJECT_ID=your-project-id
FIREBASE_PROJECT_ID=your-firebase-project-id
GCP_PROJECT_NUMBER=your-project-number

# === Firebase Web API Key (for Python client) ===
# Found in: Firebase Console → Project Settings → General → Your apps
FIREBASE_WEB_API_KEY=AIza...your-key-here

# === Firebase Auth Domain ===
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# === Firebase Storage Bucket ===
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app

# === Firestore ===
FIRESTORE_REGION=asia-south1 (or your chosen region)

# === Gemini API Key ===
# Store in Secret Manager with name: GEMINI_API_KEY
# For local client: Add to client/config.yaml
GEMINI_API_KEY_LOCATION=Secret Manager → GEMINI_API_KEY

# === Future: Cloud Run Backend URL ===
# Will be filled after Phase 1 deployment
BACKEND_URL=https://your-backend.run.app

# === Notes ===
# IMPORTANT: Copy this to firebase-credentials.txt and fill in real values
# firebase-credentials.txt is in .gitignore and will NOT be committed

